<div class="container">
  <div class="users">
    <% @users.each do |user| %>
      <div class="user">
        <%= link_to user.name, "/users/#{user.id}", class: "user-name" %>
      </div>
    <% end %>
  </div>
</div>