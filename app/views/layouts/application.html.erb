<!DOCTYPE html>
<html>
  <head>
    <title>Musicolor</title>
    <meta name="viewport" content="width=device-width,initial-scale=1.0,minimum-scale=1.0,user-scalable=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    
    <script src="https://kit.fontawesome.com/53d883e4e5.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if current_page?(new_post_path) || current_page?(new_user_path) || current_page?(login_path) || current_page?(root_path) %>
      <div class="background-100vh">
        <%= render 'layouts/header' %>
        <%= render 'layouts/flash' %>
        <%= yield %>
      </div>
    <% else %>
      <div class="background">
        <%= render 'layouts/header' %>
        <%= render 'layouts/flash' %>
        <%= yield %>
      </div>
    <% end %>
  </body>
</html>
